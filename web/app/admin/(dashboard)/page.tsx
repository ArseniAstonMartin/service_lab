export default function AdminHomePage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Admin</h1>
      <p className="text-muted-foreground">
        Overview dashboard — built out in a later task. Use the nav above for
        Orders, the Review Queue, Compatibility and Pricing.
      </p>
    </div>
  );
}
